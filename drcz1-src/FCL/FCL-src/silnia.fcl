((n)
 start

 ( start   (let wynik 1)
           (goto test))

 (  test   (if (= n 0) koniec {else} petla))

 ( petla   (let wynik (* wynik n))
           (let n (- n 1))
           (goto test))
   
 (koniec   (return wynik)))